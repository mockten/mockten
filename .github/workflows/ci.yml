name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  build_ecfront:
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: ecfront2

    steps:
      - uses: actions/checkout@v1


      - name: Clean install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build:dev

  push_snapshot_container_ecfront:
    needs: [build_ecfront]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: ecfront2
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/ecfront:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_ecpay:
    runs-on: ubuntu-22.04
    # if: contains(github.event.head_commit.modified, 'ecpay/')
    defaults:
      run:
        working-directory: ecpay
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_ecpay:
    needs: [build_ecpay]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: ecpay
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/ecpay:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG


  build_ranking:
    runs-on: ubuntu-22.04
    # if: contains(github.event.head_commit.modified, 'ranking/')
    defaults:
      run:
        working-directory: ranking
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_ranking:
    needs: [build_ranking]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: ranking
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0

    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/ranking:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_sale:
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: sale
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_sale:
    needs: [build_sale]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: sale
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/sale:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_shipment:
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: shipment
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_shipment:
    needs: [build_shipment]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: shipment
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/shipment:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG



  build_searchitem:
    runs-on: ubuntu-22.04
    # if: contains(github.event.head_commit.modified, 'searchitem/')
    defaults:
      run:
        working-directory: searchitem
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_searchitem:
    needs: [build_searchitem]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: searchitem
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/searchitem:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_product:
    runs-on: ubuntu-22.04
    # if: contains(github.event.head_commit.modified, 'product/')
    defaults:
      run:
        working-directory: product
    steps:
    - uses: actions/checkout@v1
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: Build
      run: go build -v .

  push_snapshot_container_product:
    needs: [build_product]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: product
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/product:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG


  push_snapshot_container_uam:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: uam
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/uam:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG
  push_snapshot_container_apigw:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: apigw
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/apigw:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG
  push_snapshot_container_minio:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: minIO
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/minio:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_meilisearch:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: meilisearch
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/meilisearch:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_mysql:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: mysql
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/mysql:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_sync:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: sync
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/sync:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_redis:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: redis
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/redis:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_geocoding:
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: geocoding
    steps:
      - uses: actions/checkout@v1
      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - name: Init module (only on CI)
        run: |
          if [ ! -f go.mod ]; then
            go mod init github.com/${{ github.repository }}/geocoding
            go get github.com/go-sql-driver/mysql@v1.8.1
            go mod tidy
          fi
      - name: Build
        run: go build -v .
        env:
          GOWORK: off

  push_snapshot_container_geocoding:
    needs: [build_geocoding]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: geocoding
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/geocoding:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  build_dashboard:
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: monitoring/dashboard
    steps:
      - uses: actions/checkout@v1


      - name: Clean install dependencies
        run: npm ci || npm install

  push_snapshot_container_dashboard:
    needs: [build_dashboard]
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: monitoring/dashboard
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1
    
    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/dashboard:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_cart:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1

    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/cart:latest"
        docker build -f cart/Dockerfile -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_recommendation:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: recommendation
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1

    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/recommendation:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

  push_snapshot_container_airflow:
    if: ${{ github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-22.04
    defaults:
      run:
        working-directory: airflow
    env:
      MAJOR_VERSION: 0
      MINOR_VERSION: 0
    steps:
    - name: Checkout the repository
      uses: actions/checkout@v1

    - name: Log in to GitHub Container Registry
      run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

    - name: Build and push container
      run: |
        export IMAGE_TAG="ghcr.io/${{ github.repository }}/airflow:latest"
        docker build -t $IMAGE_TAG .
        docker push $IMAGE_TAG

